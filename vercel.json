{
  "buildCommand": "pnpm --filter @workspace/hr-dashboard run build && mv artifacts/hr-dashboard/build frontend-build",
  "outputDirectory": "frontend-build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
