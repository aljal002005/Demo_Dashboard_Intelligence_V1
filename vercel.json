{
  "buildCommand": "pnpm --filter @workspace/hr-dashboard run build",
  "outputDirectory": "artifacts/hr-dashboard/build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
