{
  "buildCommand": "cd artifacts/hr-dashboard && npx vite build",
  "outputDirectory": "artifacts/hr-dashboard/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
